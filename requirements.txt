# Core astronomy
astropy>=6.0
astroquery>=0.4.7

# AI
anthropic>=0.25.0

# Weather & HTTP
httpx>=0.27.0

# Utilities
numpy>=1.26.0
python-dotenv>=1.0.0

# ML (seeing predictor)
xgboost>=2.0.0

# EDA notebook (api/ml/notebooks/seeing_eda.ipynb) — dev only
pandas>=2.0.0
matplotlib>=3.7.0

# ERA5 seeing-training data prep (api/ml/era5.py) — dev only, not needed
# by the API server or the inference path (imported lazily).
cdsapi>=0.7.0
xarray>=2024.1.0
netCDF4>=1.6.0

# Web API
fastapi>=0.95.0
uvicorn[standard]>=0.22.0

# Tests
pytest>=8.0.0
pytest-asyncio>=0.23.0
# Production API base. The frontend calls the Cloudflare Worker directly
# (cross-origin; the Worker sends CORS * and handles OPTIONS preflight).
# We do NOT route /api through Cloudflare Pages _redirects because Pages
# rewrites to an external URL only proxy GET — POST returns 405.
VITE_API_BASE=https://zenith-api.zuha-hassen.workers.dev
